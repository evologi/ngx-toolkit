import { UrlValidator } from './url-validator.directive'

describe('UrlValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlValidator()
    expect(directive).toBeTruthy()
  })
})
