import { AlphabetDirective } from './alphabet.directive'

describe('AlphabetDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetDirective()
    expect(directive).toBeTruthy()
  })
})
