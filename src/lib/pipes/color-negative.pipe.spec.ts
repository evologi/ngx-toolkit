import { ColorNegativePipe } from './color-negative.pipe'

describe('ColorNegativePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorNegativePipe()
    expect(pipe).toBeTruthy()
  })
})
